# Brand Voice

This is the foundation file. Everything else in the system references this.

**How to fill this in:** Drop 5-10 of your best past emails into `examples/`. Then ask Claude: *"Read my examples and help me fill in brand-voice.md by reverse-engineering what I actually do."* Iterate from there.

---

## Who is the voice?

Is the voice a single person (founder, CEO), a character, or a brand collective?

`{{FILL IN — one or two sentences. Describe the persona behind the voice. Is it a named founder? A character? A collective "we"? What's the vibe — formal, conversational, irreverent?}}`

---

## Three pillars of the voice

Pick three adjectives that define your voice. Not ten. Three.

1. `{{ADJECTIVE 1}}` — what this looks like in practice: `{{one sentence}}`
2. `{{ADJECTIVE 2}}` — what this looks like in practice: `{{one sentence}}`
3. `{{ADJECTIVE 3}}` — what this looks like in practice: `{{one sentence}}`

---

## Voice IS

List 8-12 qualities your voice has. Be specific.

- `{{e.g., "Confident but not arrogant"}}`
- `{{e.g., "Direct but not harsh"}}`
- `{{e.g., "Uses humor as a delivery mechanism, not as the point"}}`
- ...

## Voice is NOT

List 6-10 things your voice avoids.

- `{{e.g., "Generic motivational fluff"}}`
- `{{e.g., "Preachy or condescending"}}`
- `{{e.g., "Corporate hedging"}}`
- ...

---

## Sentence structure

How do your sentences actually move?

- Average sentence length: `{{short / medium / mixed / long}}`
- Fragments allowed? `{{yes/no — if yes, for what purpose}}`
- Contractions? `{{always / sometimes / never}}`
- Direct address (you/your)? `{{yes/no}}`
- Formatting moves: `{{e.g., "Bold key phrases for emphasis. Short one-line paragraphs for rhythm."}}`

---

## Vocabulary patterns

Words and phrases you use vs. the generic versions. List 10-20.

| You say | Not |
|---|---|
| `{{your signature term}}` | `{{the generic term you avoid}}` |
| `{{your signature term}}` | `{{the generic term you avoid}}` |
| `{{your signature term}}` | `{{the generic term you avoid}}` |
| | |
| | |

---

## Signature phrases

Phrases you use repeatedly — or *should* use more often because they're yours.

- `{{e.g., "Here's what I mean..."}}`
- `{{e.g., "Let me show you..."}}`
- `{{e.g., "If they can do it, why not you?"}}`
- `{{e.g., "This isn't about X. It's about Y."}}`
- ...

---

## Opening moves

How do your emails actually start? Avoid "Hi [name]" generic openers. What are your 3-5 go-to openings?

1. `{{e.g., "Jump straight to the tension: 'Most people get this wrong.'"}}`
2. `{{e.g., "Start with a specific dollar amount or number"}}`
3. `{{e.g., "Open with a reader question"}}`
4. ...

## Closing moves

How do your emails actually end?

1. `{{e.g., "Direct CTA with no hedging"}}`
2. `{{e.g., "PS that reframes the whole email"}}`
3. ...

---

## Tone variations

Does your voice shift across different contexts? If so, how?

- **Promotional emails:** `{{describe the shift}}`
- **Nurture/story emails:** `{{describe the shift}}`
- **Announcements:** `{{describe the shift}}`
- **Replies/conversational:** `{{describe the shift}}`

---

## The gut-check questions

When drafting, ask: does this email pass these checks?

- `{{e.g., "Does this sound like [name] holding court, or like a press release?"}}`
- `{{e.g., "Am I giving permission or creating guilt?"}}`
- `{{e.g., "Am I being specific or generic?"}}`
- `{{e.g., "Would a contrarian reader roll their eyes at this?"}}`
- ...

---

## Examples to learn from

Point Claude to your best past emails. Claude should match these, not invent something new.

- See `examples/` folder
- Drop at least 5 emails in there before expecting the system to sound like you
- Star the absolute best ones at the top of this list:

1. `{{filename of best example}}`
2. `{{filename of second-best example}}`
3. `{{filename of third-best example}}`

---

## A note on filling in this file

The voice teardown is the hardest part of this system. If you read your own examples and can't tell what makes them yours, that's normal — your voice is invisible to you because you're inside it.

The fastest fix is another set of eyes. If you get stuck here, the [workshop](../README.md#want-help-closing-the-trust-gap) does the teardown live on your real emails. Otherwise: drop more examples in, ask Claude to read them all and propose a draft of this file, then push back on anything that sounds like a description of someone else.

# Company Context

The big-picture positioning, philosophy, and values that shape everything you write. The system reads this to understand *why* the brand exists, not just what it sells.

---

## What you actually do

In one sentence:
`{{one clear sentence describing what you do, for whom, and what outcome}}`

In a short paragraph (2-4 sentences):
`{{elaborate — what you do, who for, what makes it different}}`

---

## The core belief

What's the one belief that organizes everything you write?

`{{one sentence that captures the guiding conviction of the brand — the thing that, if you stopped believing it, the brand would stop making sense}}`

---

## What you're against

Brands that stand for something are often stronger when they name what they're *against*. What's the conventional wisdom you reject?

`{{one or two sentences — name the common approach in your category that you think is wrong, and why}}`

---

## Core philosophy / core frameworks

The recurring mental models you refer back to in emails. Name them and define them.

### `{{Framework 1 Name}}`
`{{one-paragraph description}}`

### `{{Framework 2 Name}}`
`{{one-paragraph description}}`

### `{{Framework 3 Name}}`
`{{one-paragraph description}}`

---

## Signature philosophies (quotable)

Short, punchy principles the system can reference or paraphrase. Include actual quotes when you have them with sources.

- `{{quoted principle 1}}` — `{{source}}`
- `{{quoted principle 2}}` — `{{source}}`
- `{{quoted principle 3}}` — `{{source}}`
- ...

---

## Product ecosystem

How the offers connect. When the system mentions one product, it should understand what the next logical step is.

```
{{Map your own ladder. Example structure:

Entry point:   {{free or low-commitment thing that gets them in the door}}
Next step:    {{first paid offer}}
Then:         {{expansion offer(s)}}
Premium:      {{high-ticket or advanced tier}}

The point is to show the system how your offers relate so it doesn't pitch the premium offer to a cold lead.}}
```

---

## What you don't do

What do you *not* cover, so the system doesn't drift into adjacent topics that aren't yours?

- `{{topic you refuse to cover}}`
- `{{adjacent space you stay out of}}`
- `{{claim you won't make}}`
- ...

---

## Values in communication

The meta-rules that shape how every email feels.

1. **`{{Value name}}`** — `{{what it means for writing}}`
2. **`{{Value name}}`** — `{{what it means for writing}}`
3. **`{{Value name}}`** — `{{what it means for writing}}`
4. **`{{Value name}}`** — `{{what it means for writing}}`
5. **`{{Value name}}`** — `{{what it means for writing}}`

---

## The brand story (canonical version)

The 200-400 word version of your origin / why-this-exists that the system can pull pieces from when context is needed.

`{{Write the full story here. What problem did you see that no one else was solving? What belief made you start? Who have you helped and what changed for them? End with what you're inviting the reader into.

Keep it in your actual voice — this passage is reference material the system will paraphrase or pull from, so if it sounds like a press release here, it will sound like a press release in every email that references it.}}`

---

## Tone variations by product

Different offers lean on slightly different voice registers. Note them here.

### `{{Offer 1}}` buyer tone
- `{{how the voice shifts for this audience — what to emphasize, what to avoid}}`

### `{{Offer 2}}` buyer tone
- `{{how the voice shifts for this audience}}`

### `{{Offer 3}}` buyer tone
- `{{how the voice shifts for this audience}}`
